@using System.Web.Optimization;
@model Claro.SIACU.Web.WebApplication.Areas.Dashboard.Models.PostpaidDataAccount.AccountAnnotationModel

<div id="ContentAccountAnnotationList">
    <table class="table  table-striped table-bordered table-hover table-responsive claro-modal-tabla" id="tblAccountAnnotationList">
        <thead class="claro-bg-primary">
            <tr>
                <th class="col-sm-1 sorting">C&#243;digo Anotaciones</th>
                <th class="col-sm-1 sorting">Estado</th>
                <th class="@ViewBag.hiddenTobe">Prioridad</th>
                <th class="col-sm-2 sorting">Descripci&#243;n Breve</th>
                <th class="@ViewBag.hiddenTobe">Usuario Seguimiento</th>
                <th class="col-sm-2 sorting">Fecha Seguimiento</th>
                <th class="@ViewBag.hiddenTobe">Acci&#243;n Seguimiento</th>
                <th class="col-sm-1 sorting">Descripción Completa</th>
                <th class="hidden"></th>
                <th class="hidden"></th>
                <th class="hidden"></th>
                <th class="hidden"></th>
                <th class="hidden"></th>
            </tr>
        </thead>
        <tbody>
            @if (Model.listAnnotationPast != null)
            {
                foreach (var item in @Model.listAnnotationPast)
                {
                    <tr>
                        @*@if (ViewBag.hiddenTobe != "")
                            {
                                <td class="col-sm-1">@item.Code</td>
                                <td class="col-sm-1">@item.Status</td>
                                <td class="col-sm-2">@item.Description</td>
                                <td class="col-sm-1"><a id="lknDescription" role="button">Ver</a></td>
                            }
                            else
                            {

                                <td class="col-sm-1">@item.Code</td>
                                <td class="col-sm-1">@item.Status</td>
                                <td class="col-sm-1">@item.Priority</td>
                                <td class="col-sm-2">@item.Description</td>
                                <td class="col-sm-1">@item.UserTracing</td>
                                <td class="col-sm-2">@item.DateTracing</td>
                                <td class="col-sm-2">@item.ActionTracing</td>
                                <td class="col-sm-1"><a id="lknDescription" role="button">Ver</a></td>


                            }*@
                        <td class="col-sm-1">@item.Code</td>
                        <td class="col-sm-1">@item.Status</td>
                        <td class="@ViewBag.hiddenTobe">@item.Priority</td>
                        <td class="col-sm-2">@if (item.Description != null)
                        {@item.Description.Split('|')[0].ToString()}</td>
                        <td class="@ViewBag.hiddenTobe">@item.UserTracing</td>
                        <td class="col-sm-2">
                            @if (item.DateTracing != null)
                            { @item.DateTracing }
                        </td>
                        <td class="@ViewBag.hiddenTobe">@item.ActionTracing</td>
                        <td class="col-sm-1"><a id="lknDescription" role="button">Ver</a></td>
                        <td class="hidden">@item.NumberTickler</td>
                        <td class="hidden">@item.DateClosing</td>
                        <td class="hidden">@item.DateOpening</td>
                        <td class="hidden">@item.DateModify</td>
                        <td class="hidden">@if (item.Description != null)
{@item.Description.Split('|')[1].ToString()}</td>

                    </tr>
                }
            }
        </tbody>
    </table>

    <br />
    <label class="control-label">*Mostrando las anotaciones de los últimos 12 meses.</label>
</div>
@Scripts.Render("~/bundles/Content/Lib/BloqueoF12")
@Scripts.Render("~/bundles/Dashboard/PostpaidDataAccount/PostpaidAccountAnnotationList")